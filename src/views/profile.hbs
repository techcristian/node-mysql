
{{#if user}}
<div class="container p-4">
  <div class="row">
    <div class="col-md-4 mx-auto">
      <div class="card text-center bg-dark text-white">
        <div class="card-body">
          <h4>Welcom {{user.fullname}}</h4>
          <h4>{{user.username}}</h4>
          <a href="/links/add">
            Save your Links!
          </a>
        </div>
      </div>
    </div>
  </div>
</div>
{{/if}}